{
  "name": "claude-bioflow-adapter",
  "version": "0.1.0",
  "description": "NATS <-> Claude Code bridge. Runs inside each user devcontainer.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pantheon-adapter": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node20 --clean --out-dir dist",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.2.116",
    "@anthropic-ai/claude-code": "2.1.116",
    "nats": "2.29.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
